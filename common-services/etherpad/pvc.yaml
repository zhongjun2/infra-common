---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ether-data-vol
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50Gi
  storageClassName: ssd
  volumeMode: Filesystem