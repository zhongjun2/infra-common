apiVersion: secrets-manager.tuenti.io/v1alpha1
kind: SecretDefinition
metadata:
  name: grafana-secrets
spec:
  name: grafana-secrets
  keysMap:
    password:
      path: secrets/data/infra-common/grafana
      key: grafana_password
    username:
      path: secrets/data/infra-common/grafana
      key: grafana_username
    grafana_ini:
      path: secrets/data/infra-common/grafana
      key: grafana_ini
