apiVersion: v1
kind: Service
metadata:
  labels:
    app: grafana
    name: grafana
  name: grafana
  namespace: monitoring
spec:
  externalTrafficPolicy: Cluster
  ports:
  - name: cce-service-0
    nodePort: 31919
    port: 3000
    protocol: TCP
    targetPort: 3000
  selector:
    app: grafana
  sessionAffinity: None
  type: LoadBalancer
