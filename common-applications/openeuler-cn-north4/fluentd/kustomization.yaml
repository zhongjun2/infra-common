resources:
  - ../../../common-services/fluentd
  - secrets.yaml
commonLabels:
  app: openeuler-cn-north4-fluentd
configMapGenerator:
  - name: fluentd-config
    files:
      - kubernetes.conf
patchesStrategicMerge:
  - daemonset.yaml
namespace: kube-system


