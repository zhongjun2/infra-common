# Deployment 
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sca-gateway
  namespace: sca
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sca-gateway
  template:
    metadata:
      labels:
        app: sca-gateway
    spec:
      imagePullSecrets:
        - name: sca-sec
      containers:
      - name: sca-gateway
        image: 172.16.1.156:5000/cloudsca/gateway-service:v1
        imagePullPolicy: IfNotPresent
        ports:
        - name: app-port
          containerPort: 8015
        env:
          - name: MY_POD_NAME
            valueFrom:
              fieldRef:
                fieldPath: metadata.name
          - name: SERVICE_NAME
            value: sca-gateway
          - name: EUREKA_SERVER
            value: "http://eureka.sca.svc.cluster.local:9000/eureka/,eureka1.sca.svc.cluster.local"
          - name: APP_ENV
            value: test
          - name: PACKAGE_VERSION
            value: 1.0-SNAPSHOT
          - name: MAX_HEAP_MEM
            value: 3072m
          - name: INIT_HEAP_MEM
            value: 512m
          - name: CMS_IOF
            value: "75"
        resources:
          requests:
            cpu: 200m
            memory: 1024Mi
          limits:
            cpu: 800m
            memory: 2G
---
# Service 
apiVersion: v1
kind: Service
metadata:
  name: sca-gateway
  namespace: sca
  labels:
    app: sca-gateway
spec:
  type: ClusterIP
  ports:
    - port: 8080
      name: gateway-app
      protocol: TCP
      targetPort: 8080
  selector:
    app: sca-gateway
