resources:
  - ../../../common-services/vault
  - namespace.yaml
  - pvc.yaml
  - ingress.yaml
  - secrets.yaml
commonLabels:
  app: control-vault
namespace: vault
patchesStrategicMerge:
  - statefulset.yaml

