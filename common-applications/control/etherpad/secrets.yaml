apiVersion: secrets-manager.tuenti.io/v1alpha1
kind: SecretDefinition
metadata:
  name: ether-secret
spec:
  name: ether-secret
  keysMap:
    admin_password:
      path: secrets/data/infra-common/etherpad
      key: admin_password
    db_password:
      path: secrets/data/infra-common/etherpad
      key: db_password
    user_password:
      path: secrets/data/infra-common/etherpad
      key: user_password
