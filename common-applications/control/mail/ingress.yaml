apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-issuer
  name: mailmanweb-ingress-for-osinfra
spec:
  tls:
    - hosts:
        - mailweb.osinfra.cn
      secretName: mailman-cert-secrets
  rules:
    - host: mailweb.osinfra.cn
      http:
        paths:
          - backend:
              serviceName: mailman-web-service
              servicePort: 80
            path: /
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-issuer
  name: mailmanweb-ingress-for-openhetuengine
spec:
  tls:
    - hosts:
        - mailweb.openhetuengine.org
      secretName: mailman-cert-secrets-foropenhetu
  rules:
    - host: mailweb.openhetuengine.org
      http:
        paths:
          - backend:
              serviceName: mailman-web-service
              servicePort: 80
            path: /
---